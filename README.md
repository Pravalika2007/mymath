# mymath
Mathematical Utility Tool in Python using a reusable class. The program performs multiple operations through a single method like sum of first n natural numbers, prime numbers generation, Fibonacci series, and factorial calculation. Users select the required operation from the keyboard.
